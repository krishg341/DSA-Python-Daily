class Solution:
    def countNegatives(self, grid: List[List[int]]) -> int:
        g=0
        for i in grid:
            for j in i:
                if j<0:
                    g+=1
        return g

        
